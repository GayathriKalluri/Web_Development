API's are the link between frontend and backend
we will be studying

what is an API
clients and servers
requests and responses
json data
the url's we use to access apis
fetching data
handling data
promises

what is an api?
Application programming interface
restaurant analogy:
gone with family and to place order and receive food
and in kitchen everything is happening without knowing us
-> an api is any tool that helps connect your program with someone else's program.
api examples:
getting data from server -> the server hosts "an api" - exposed "endpoints" we can access for getting data from the server
note that the server doesn't give us access to everything, just the things they want us to have
pre-written code that does cool stuff:
dom api(.getElementById)
array methods API (.filter,.map etc)
3rd-party packages

how would you describe an API in your own words
an api is any tool that helps connect your program with someone else's program.

can you think of an example of an API you've used?
boredAPI - (getting data from server)
local storage - 

what is a request?
when a device asks for a resource (data,image,HTML page, authentication token, etc..)
requires a connection to the internet somehow

what is a responses?
the reply to the request
could contain the resource(html,json data etc) asked for by the client 
could contain a response saying the client isn't authorized to receive the resource

3things client might request from the server?
json array of suggested videos
video stream
index.html

main job of server?
receive a request from a client and return a response.

3-digit server response code?
500 -> internal server error

JSON Review:
ex:- this is format for data
javascript object notation
{
    "name": "Joe schmoe",
    "age":42,
    "gender":"male",
    "hobbies":[
        "skiing"
    ]
}

can have single object or array of objects

URLs and Endpoints:
the makeup of a URL
1)the base url => https://scrimba.com/api
2)endpoints => specific resource that we want to receive
/courses
/users
/templates
/resourses/challenges

fetching with .then():-
fetch().then()

awit and async fetching 
error handling

status code basics
200-299 -> successful response : true
404 -> not found : false
500 -> server error :false

try/catch: catches excpetions and errors that occur during the execution of the code,
including network errors and other unexpected issues

response.ok => checks the success of the HTTP response status,which might not throw an error
but still indicates a failure

methods:
get -> getting data
post -> posting data
put -> updating data
delete -> deleting data
patch and options

when we do the fetch request get is the default method and we have two parameters in fetch method

we can improve the response by using headers
headers contain: 
extra info about the request
authentication 
the type of data being sent
this is not an exhaustive list
